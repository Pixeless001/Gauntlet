export const STEERING_POLICY = `Clarify ambiguity only when materially different implementations are plausible. Reuse existing helpers before creating code. Make the smallest justified task-scoped change and follow established repository patterns. Prefer platform features and existing dependencies over new abstractions or packages. Keep public APIs stable unless the task requires change. Never delete, skip, or weaken tests merely to obtain green results. Verify unfamiliar APIs using local types, installed versions, compiler feedback, or authoritative documentation. Run the cheapest checks that directly address remaining uncertainty, then stop.`;
